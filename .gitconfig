[core]
    editor = 'emacs -nw'
    pager = less
    excludesfile = ~/.gitignore_global

[color]
    ui = auto
    grep = auto

[alias]
    br = branch
    st = status
    co = checkout
    ci = commit
    k = !gitk
    g = gui
    last = log -1 HEAD
    onel = log --oneline --decorate
    onelx = log --oneline --decorate -30 HEAD
    graph = log --oneline --graph --pretty='format:|%C(yellow) %h | %C(red) %ad | %C(green) %an | %C(reset) %s %d'

[user]
    name = PalaceChan
    email = XXX

[merge]
    conflictstyle = diff3

[credential]
    helper = cache --timeout=3600

[init]
	defaultBranch = master
    
[github]
    user = PalaceChan
    oauth-token = YYY
